# TUTORial — Flask prototype
#
# No system dependencies. Video generation happens in Sora, so there is no
# ffmpeg, no local rendering and no GPU to provision.

Flask>=3.0
anthropic>=1.0             # the lesson plan, the flipbook SVGs, the checkpoints
openai>=3.0                # Sora animates the flipbook into each video segment
requests>=2.31             # only used to fetch the notebook fonts on first run
